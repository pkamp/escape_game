import 'package:flutter/material.dart';

class TabletDialog extends StatelessWidget {
  const TabletDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      
    );
  }
}
